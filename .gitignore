# Node
node_modules/
dist/
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Editors
.vscode/
.idea/
*.swp
*~
.DS_Store

# Test artefacts
coverage/
.nyc_output/

# Local agents.yaml (operators copy agents.yaml.example → agents.yaml and edit)
agents.yaml

# Agent worktrees. `.claude/worktrees/` holds a full checkout per background
# agent -- 527 MB here on 2026-09-05 -- and it is neither tracked nor ignored,
# so it lands in the docker build context of every local `cli.sh build`.
.claude/
