# Devplan — cerase-acp v0.1 (PoC milestone M4)

**Status**: working document
**Started**: 2026-05-24
**Umbrella milestone**: `cerase/devplan/poc.md` §M4 — `cerase-bridge` (in-house DM connector) + OpenCode agent + DM round-trip

`cerase-acp` is the in-house Discord-to-ACP bridge for Cerase. It is the
single TypeScript artefact in the Cerase stack and is intentionally split
into its own repository so its build pipeline (npm, tsc, vitest) does not
leak into the otherwise-bash/Python/PHP `cerase/` repo. Cerase consumes
this repo as a published Docker image (`cerase-acp:<tag>`), the same way
it consumes OpenCode and LiteLLM.

v0.1 delivers the PoC slice of M4: DM-only Discord ingestion, ACP v1
stdio session per `(user, agent)`, `[turn_meta]` injection,
crash-recovery, allowlist enforcement, and a `BRIDGE_E2E_TEST` injection
endpoint for automated end-to-end tests in the cerase repo. v0.2+ will
add (post-M4): permission gating UX refinements, multi-platform support
(Slack, Telegram) if Tier-0 customers require it, and richer attachment
handling.

**UX scope** (from `cerase/devplan/poc.md` §M2):
- DM-only Discord. No guild-channel listening, no slash commands, no
  buttons, no "control panel" surface. The user talks like to a
  colleague.
- One long-lived ACP session per `(user, agent)` pair. Survives bridge
  restart transparently.
- Per-turn `[turn_meta: gap=…, lang=…]` block prepended to each
  `session/prompt`, read by the agent's system prompt
  (`cerase/agent-runtime/agent/srv/AGENTS.md`).
- Per-agent `user_id` allowlist enforced by the bridge. Unauthorised DM
  → polite refusal reply, an `event_log` row in Cerase Postgres (M5
  wires the row; v0.1 just emits a structured log line that M5 will
  ingest).

**Inspiration**: structural pattern adapted from
[Open-ACP/OpenACP](https://github.com/Open-ACP/OpenACP) — specifically
the separation of `session-manager` / `prompt-queue` / `turn-context` /
adapter-primitive concepts in `src/core/sessions/` and
`src/core/adapter-primitives/`. We **do not** import OpenACP code or
follow its plugin/monorepo architecture (overkill for a single-platform
PoC). We **do not** adopt its forum-thread / slash-command / button-
permission UX (incompatible with Cerase's colleague-style positioning).
Patterns worth porting: 2000-char Discord message chunking with
continuation markers, per-channel rate-limited send queue, streaming
draft manager that buffers `agent_message_chunk` events and flushes on
sentence-boundary / timer / size threshold, per-session prompt queue
that serialises concurrent user turns through a single ACP child.

---

## M1 — Repo init + project layout

**Why**: empty repo. Everything that follows needs a `package.json`,
`tsconfig.json`, vitest config, and a clear source layout. No code in
this milestone — just the scaffold.

**Scope**:
- `package.json` pinning `discord.js@^14.26.x`,
  `@agentclientprotocol/sdk@0.22.1` (exact), `zod@^4.x`, `yaml@^2.x`,
  `pino@^10.x`. Dev: `typescript@~5.9`, `tsx@^4.x`, `vitest@^4.x`,
  `@types/node@^20.x`. Scripts: `build`, `test`, `lint` (placeholder),
  `dev`.
- `tsconfig.json` strict mode, `module: NodeNext`, `target: ES2022`.
- `vitest.config.ts` minimal config; tests in `src/**/*.test.ts`.
- `.gitignore` for Node + editors + macOS.
- `README.md` linking back to `cerase/` as the umbrella repo.
- `src/index.ts` placeholder so `npm run build` succeeds on a fresh
  clone.

**Tasks**:
- [x] `devplan/v0.1.md` (this file).
- [x] `package.json` with the pins above + `engines: node >= 20`.
- [x] `tsconfig.json` strict + ES2022 + NodeNext modules.
- [x] `vitest.config.ts` minimal.
- [x] `.gitignore` (Node, IDEs, macOS).
- [x] `README.md` short, links to `cerase/` repo.
- [x] `src/index.ts` placeholder (replaced in M5 with the real wire-up).
- [x] `npm install` succeeds on a clean clone.
- [x] `npm run build` succeeds (compiles the placeholder).
- [x] `npm test` succeeds (vitest with `--passWithNoTests`).

**Exit gate**: clean clone → `npm ci && npm run build && npm test` all
green; `node dist/index.js` prints the not-yet-implemented banner and
exits 0. ✅ CLOSED 2026-05-24 (root commit `873a822`).

**Effort**: 0.5 day.

---

## M2 — Config + allowlist

**Why**: every subsequent milestone needs to know which agents are
configured, which Discord token belongs to which agent, and which
`user_id`s can DM each bot. The config is the bridge's contract with
the operator.

**Scope**:
- `src/config.ts`: load `agents.yaml` from path (default
  `/etc/cerase-acp/agents.yaml`), resolve `${env:VAR}` placeholders
  against `process.env`, parse with `yaml`, validate with `zod`. Schema
  matches the one in `cerase/devplan/poc.md` §M4 (agents list with
  `id`, `bot_token`, `allowed_users`, `spawn.command`, `spawn.args`;
  global `session.idle_timeout_minutes`, `session.max_concurrent`).
- `src/allowlist.ts`: pure function
  `isAllowed(agentConfig, discordUserId) → boolean`. Used by the
  Discord adapter (M5) to gate `messageCreate` events.
- `agents.yaml.example` at repo root for operators.

**Tasks**:
- [x] `src/config.ts` with zod schema + env-substitution + load function.
- [x] `src/allowlist.ts` with a pure check function.
- [x] `src/config.test.ts` covering: valid YAML round-trips, missing
      file → clear error, malformed YAML → clear error, missing env var
      in `${env:…}` → clear error, schema violation → clear error,
      plus duplicate-id detection and agent-id portability check (no
      spaces, alphanumeric + '-' only — keeps `docker exec` happy).
- [x] `src/allowlist.test.ts` covering: user in list → true, user not
      in list → false, agent not found → throws, cross-agent isolation
      (an allowed user on one agent is rejected on another), empty
      `allowed_users` list → false (parked template).
- [x] `agents.yaml.example` mirroring the cerase devplan §M4 schema.

**Exit gate**: `npm test` green with the new tests (15 cases); an
example YAML in the repo root that an operator can copy and edit.
✅ CLOSED 2026-05-24.

**Effort**: 0.5 day.

---

## M3 — ACP session manager + prompt queue

**Why**: the long-lived per-`(user, agent)` session is the central
abstraction of the bridge. Everything else (turn-meta, streaming,
Discord adapter) plugs into it. Must be testable in isolation — no
Discord, no LLM, no docker — by talking to a fake ACP child that
replies deterministically to stdio NDJSON.

**Scope**:
- `src/session-manager.ts`: `SessionManager` class with a
  `Map<sessionKey, Session>` registry where `sessionKey =
  ${agent_id}:${user_id}`. Lazy-spawn `opencode acp` via configured
  `spawn.command + spawn.args` (in PoC: `docker exec -i
  cerase-agent-<id> opencode acp`). ACP client wraps stdin/stdout via
  `@agentclientprotocol/sdk@0.22.1`. On child exit: log + remove from
  map; next user turn re-spawns transparently. Idle-timeout via
  `setTimeout` reset on each turn, kills the child after configured
  minutes of inactivity. `prompt(text)` enqueues + returns the streamed
  reply.
- `src/prompt-queue.ts`: per-session FIFO. If user sends turn #2 while
  turn #1 is in flight, queue it. ACP is single-threaded per session.
- `src/__tests__/fake-acp-child.ts`: a tiny Node script that reads
  stdin NDJSON, emits `initialize` response with `protocolVersion: 1`,
  responds to `session/new`/`session/prompt` with deterministic
  fixtures, exits cleanly on `session/cancel`.

**Tasks**:
- [x] `src/session-manager.ts` with the lifecycle above (~180 LOC).
- [x] `src/prompt-queue.ts` (~50 LOC).
- [x] `src/__tests__/fake-acp-child.mjs` test fixture — plain .mjs (no
      TypeScript) so the spawn loop has zero transpilation overhead;
      env knobs FAKE_REPLY / FAKE_CHUNKS / FAKE_CRASH_AFTER_PROMPT /
      FAKE_DELAY_MS_PER_CHUNK cover the test surfaces below.
- [x] `src/session-manager.test.ts`: 8 cases — lazy spawn, reuse on
      second prompt, isolation across (agent, user) keys, transparent
      respawn after crash, FIFO serialisation of concurrent prompts,
      unknown-agent rejection, shutdown() cleanup, vitest fake-timer
      driven idle-timeout kill.
- [x] `src/prompt-queue.test.ts`: 4 cases — FIFO under interleaved
      enqueues, no-overlap invariant, failing handler does not block
      the queue, size() drains to 0.
- [x] PoC permission policy: ACP `requestPermission` calls auto-cancel
      with a structured warning log (M2 UX forbids in-DM buttons; M5
      of the cerase devplan wires Filament permission UI later).
- [x] `CERASE_ACP_LOG_LEVEL` env to control pino verbosity (tests set
      it to "silent" via vitest setup; production lifts to "info").

**Exit gate**: `npm test` green (27 cases total in v0.1 so far); the
fake-child fixture proves the ACP stdio loop end-to-end without
OpenCode running. ✅ CLOSED 2026-05-24.

**Effort**: 1.5 days.

---

## M4 — Turn-meta + stream buffer + send queue

**Why**: the agent receives a `[turn_meta: gap=…, lang=…]` prefix on
every user turn (M2 system-prompt contract). The Discord side must
chunk 2000-char-limited DM replies. The streaming side must batch
`agent_message_chunk` events so we don't emit 100 Discord messages per
second.

**Scope**:
- `src/turn-meta.ts`: track last-user-message timestamp per session;
  on next turn compute `gap` (minutes/hours/days human-readable); a
  simple language heuristic from the incoming text (cheap regex check
  for common tokens — Italian/English/Spanish/French covers PoC); emit
  `[turn_meta: gap=…, lang=…]\n\n` prefix.
- `src/stream-buffer.ts`: accumulates `agent_message_chunk` events.
  Flushes when (a) a sentence boundary is reached AND ≥200 chars
  accumulated, OR (b) the buffer exceeds 1800 chars (leaving margin
  for chunking), OR (c) a 500ms timer fires after the last chunk and
  the buffer is non-empty. On flush emits the chunk to the send queue.
- `src/send-queue.ts`: per-channel FIFO. Splits messages > 1990 chars
  on sentence/newline boundaries with " ⏎" continuation marker (1990
  to leave 10 chars headroom for the marker). Spaces sends by ≥100ms
  to respect Discord's per-DM rate limit (≈5/sec safe ceiling).

**Tasks**:
- [x] `src/turn-meta.ts` + tests (15 cases). Exports formatGap,
      detectLanguage, makeTurnMetaBlock, TurnMetaTracker. Per-(agent,
      user) state. Language heuristic supports it/en/es/fr; everything
      else → "unknown" (agent's system-prompt rules pick the language
      from the message body anyway).
- [x] `src/stream-buffer.ts` + tests (7 cases). Three flush triggers
      (sentence boundary + size threshold, hard cap, idle timer). End-
      of-turn flush via end(). All recognised terminators tested
      (./!/?/newline).
- [x] `src/send-queue.ts` + tests (8 cases). chunkForDiscord() exported
      separately for reuse: prefers newline > sentence > hard-split
      boundaries; non-final chunks carry a " ⏎" marker. SendQueue
      ensures FIFO + ≥minIntervalMs spacing + auto-chunking + survives
      send() throws. drain() resolves only when no chunks pending and
      no in-flight send.

**Exit gate**: `npm test` green (56 cases total in v0.1 so far); the
chunker round-trips text >5000 chars losslessly across multiple
chunks. ✅ CLOSED 2026-05-24.

**Effort**: 1 day.

---

## M5 — Discord adapter + index.ts + test-injection endpoint

**Why**: wires the pieces from M2..M4 together behind a `discord.js`
client per agent and adds the `BRIDGE_E2E_TEST` HTTP endpoint that
cerase's `tests/e2e-discord/` (B5) uses to drive end-to-end tests
without needing real Discord traffic.

**Scope**:
- `src/discord-adapter.ts`: one `discord.js` `Client` per configured
  agent. `IntentsBitField` set to `Guilds | DirectMessages |
  MessageContent | DirectMessageReactions` (no guild-channel intents).
  `messageCreate` handler: drop bot's own messages, drop non-DM
  messages, check allowlist → either route to session manager
  (allowed) or send polite refusal (not allowed). Reply via the user's
  `DMChannel`. Attachments downloaded to
  `${workdir}/uploads/<turn-id>/` and noted in the user-turn text.
- `src/test-injection.ts`: tiny Fastify (or Node `http.createServer`)
  HTTP server listening on `:7474` ONLY when `BRIDGE_E2E_TEST=1`.
  `POST /_test/inject` accepts `{ agent_id, user_id, text }` and feeds
  the message through the same dispatcher path the Discord adapter
  uses, bypassing only the Discord client. `GET /_test/last-reply` for
  the most recent reply per `(agent_id, user_id)`. Never enabled in
  the published image's default mode.
- `src/index.ts`: load config → spawn one `DiscordAdapter` per agent →
  start `SessionManager` → wire `StreamBuffer` + `SendQueue` →
  conditionally start `TestInjectionServer`. Structured logging via
  pino. SIGTERM handler: stop accepting new messages, drain in-flight
  prompts, kill children, exit 0.

**Tasks**:
- [x] `src/dispatcher.ts`: pure Discord-agnostic pipeline (allowlist →
      session-manager → stream-buffer → send-queue). The unit of
      behavioural truth — Discord adapter and test-injection both
      drive the same dispatcher. Resolves "send target" through an
      injected callback so the dispatcher itself knows nothing about
      Discord channels.
- [x] `src/discord-adapter.ts`: thin discord.js glue. One Client per
      agent (DirectMessages + MessageContent + Guilds intents,
      Channel + Message partials), drops bot/guild messages, caches
      per-user DM channels for fast multi-chunk reply delivery. Lean
      enough to verify behaviourally via cerase's e2e-discord tier
      and BRIDGE_E2E_TEST rather than via discord.js mocks.
- [x] `src/test-injection.ts`: plain `node:http` server bound to
      127.0.0.1 (or 0.0.0.0 if needed by compose). POST /_test/inject
      drives Dispatcher.handleMessage and AWAITS completion (tests
      need determinism — production Discord path also awaits). GET
      /_test/last-reply returns the accumulated chunks for a (agent,
      user) pair with " ⏎" markers stripped.
- [x] `src/index.ts`: wires config → SessionManager + TurnMetaTracker
      → adapter table → Dispatcher (resolves send-target via the
      adapter table). Conditionally starts the test-injection server
      when BRIDGE_E2E_TEST=1. SIGINT/SIGTERM → graceful shutdown
      (stop discord clients, kill ACP children, close test server).
- [x] `src/dispatcher.test.ts` (5 cases) + `src/test-injection.test.ts`
      (4 cases) — driven against the fake-ACP child fixture.

**Exit gate**: `npm run build && node dist/index.js` exits cleanly
(rc=1) on missing config with an operator-readable fatal log; full
v0.1 suite remains green at 65/65; the BRIDGE_E2E_TEST endpoint
exercises the full dispatcher (allowlist refusal, happy path,
turn-meta prepend verified indirectly via TurnMetaTracker state).
✅ CLOSED 2026-05-24.

**Effort**: 1.5 days.

---

## M6 — Dockerfile + image build

**Why**: cerase consumes this repo as `cerase-acp:<tag>` in
`docker-compose.yml` (B1 in the cerase repo). The image must be
buildable locally during PoC and pushable to a registry in Tier-0.

**Scope**:
- Multi-stage Dockerfile: build stage on `node:20` (full image, has
  npm + git), runtime stage on `node:20-slim`. Build stage runs
  `npm ci && npm run build`. Runtime stage copies `dist/`,
  `node_modules/` (only prod deps), `package.json`. Installs
  `docker.io` package (needed for `docker exec` in PoC to spawn
  `opencode acp` in sibling containers). Uses `tini` as PID 1.
  Entrypoint: `tini -- node /app/dist/index.js`.
- `.dockerignore` excluding `node_modules`, `dist`, `.git`, etc.
- Build verification: `docker build -t cerase-acp:0.1.0-dev .` succeeds;
  `docker run --rm cerase-acp:0.1.0-dev node --version` prints the
  Node version; `docker run --rm cerase-acp:0.1.0-dev ls /app/dist`
  shows the built bundle.

**Tasks**:
- [x] `Dockerfile` multi-stage (`node:20` build, `node:20-slim`
      runtime, `tini` PID 1, `docker.io` for `docker exec`).
      Production envs (`NODE_ENV`, `CERASE_ACP_LOG_LEVEL`) and config
      path defaults baked in.
- [x] `.dockerignore` (excludes node_modules, dist, .git, agents.yaml,
      devplan, README, vitest config — only ships what build needs).
- [x] `src/dockerfile.test.ts` — 8 lightweight lint assertions that
      lock in the structural decisions (multi-stage, slim runtime,
      tini PID 1, docker.io present, npm ci not npm install, no
      host-side node_modules copy, dist/index.js as CMD, .dockerignore
      excludes the right paths).
- [x] Image builds locally as `cerase-acp:0.1.0-dev` — verified:
      - `docker run --rm cerase-acp:0.1.0-dev node --version` → v20.20.2
      - `docker run --rm --entrypoint ls cerase-acp:0.1.0-dev /app/dist`
        lists all .js bundles
      - `docker run --rm --entrypoint docker cerase-acp:0.1.0-dev --version`
        → Docker 20.10.24 (the docker CLI for spawning sibling-
        container opencode acp processes)
      - missing config → fatal pino log + rc=1
      - valid config + fake bot token → "cerase-acp starting" +
        agentCount=1 + TokenInvalid (expected) + rc=1

**Exit gate**: image builds, surfaces operator-readable failure modes
(missing config → rc=1, invalid token → rc=1 with TokenInvalid pino
log), entry point goes through tini PID 1, prod node_modules in
runtime stage (npm prune --omit=dev). Image size 692MB — mostly
docker.io; Tier-0 can drop this when the spawn command switches to
kubectl-exec. ✅ CLOSED 2026-05-24.

**Effort**: 0.5 day.

---

## M7 — Standalone CLI + bash wrapper

**Why**: the bridge daemon talks Discord on one side and ACP on the
other; both surfaces are slow to spin up for ad-hoc debugging. A
standalone CLI lets developers exercise the same pipeline
(allowlist → turn-meta → session-manager) for a single round-trip
against a configured agent (the fake-acp-child fixture for local
testing, or a real `opencode acp` once wired) without launching
Discord, compose, or even the test-injection HTTP server. A thin bash
wrapper (`scripts/cerase-acp-cli`) keeps the TypeScript surface lean
by handling the REPL loop and the remote-daemon `inject` case outside
the build artefact — TS stays a single-shot tool, bash adds the
ergonomics.

**Scope**:
- `src/cli.ts`: one subcommand for v0.1 — `prompt`. Flags:
  `--config <path>` (default `agents.yaml` in cwd),
  `--agent <id>` (required), `--user <discord-user-id>` (required),
  positional prompt text. Streams the reply to stdout as ACP
  `agent_message_chunk` updates arrive (no Discord-style chunking
  / rate-limiting — the Dispatcher's StreamBuffer + SendQueue are
  bypassed because the CLI's "channel" is just stdout). Exit 0 on
  success (including the polite-refusal path for unauthorised users —
  refusal is a valid response, not an error). Exit 1 on config errors,
  unknown agent, or ACP failures.
- `src/cli.test.ts`: vitest cases covering the prompt subcommand
  against the fake-acp-child fixture (happy path streaming, refusal
  for unauthorised user, unknown agent → exit 1 + clear message,
  missing required flag → exit 1, help text).
- `scripts/cerase-acp-cli`: bash wrapper (~80 LOC) with three
  subcommands:
    - `prompt …`  — passes args to `node dist/cli.js prompt`
    - `repl …`    — `while read` loop, each line invokes the TS CLI
      (per-turn child spawn; for testing bridge plumbing this is fine,
      and mem0+workspace handles agent-level continuity when running
      against real OpenCode)
    - `inject --remote URL --agent X --user Y "text"` — `curl POST
      /_test/inject` then `curl GET /_test/last-reply`, printing the
      captured reply text. Useful for diagnosing a running bridge
      daemon without standing up a TS process.
- Shared helper: extract `pickRefusalMessage(text)` from
  `dispatcher.ts` so both Dispatcher and CLI use one source of truth
  for the polite-refusal copy.

**Tasks**:
- [x] `src/cli.ts` (~135 LOC) — argv parsing hand-rolled (no
      commander/yargs dep), config load, allowlist gate, turn-meta
      prefix, SessionManager round-trip, stdout streaming via the
      `onUpdate` callback (no Dispatcher StreamBuffer / SendQueue —
      stdout doesn't need Discord-shaped chunking). Graceful shutdown
      via `mgr.shutdown()` in `finally`.
- [x] Extracted `pickRefusalMessage(text)` from `dispatcher.ts`; both
      Dispatcher and CLI now call it — single source of truth for the
      polite-refusal copy.
- [x] `src/cli.test.ts` (8 cases) — streaming happy path, refusal
      path, unknown agent, missing required flag, missing prompt text,
      missing config file, --help, unknown subcommand.
- [x] `scripts/cerase-acp-cli` (~115 LOC bash) — `prompt` / `repl` /
      `inject`. ANSI-C $'...' quoting on color constants so heredoc
      usage interpolates correctly.
- [x] `src/cli-wrapper.test.ts` — vitest spawning the bash script:
      `--help`, unknown subcommand, `prompt` dispatches to TS,
      `repl` two-turn stdin loop, `inject` against an in-memory
      `http.createServer` (echo daemon that upper-cases the prompt),
      `inject` against a blackhole 404 daemon.
- [x] README "Debug CLI" section with example invocations + the
      `CERASE_ACP_LOG_LEVEL=silent` recipe for silencing pino logs
      that interleave with the streamed reply.

**Exit gate**: `npm test` green (87/87); manual end-to-end via the
fake-child fixture verified — one-shot prompt streams "Ciao Paolo!
Sono il fake-acp-child..." (8 chunks, 50ms each, ~670ms wall),
unauthorised user gets Italian refusal, unknown agent → rc=1 with the
list of known agents, REPL handles two turns over piped stdin cleanly.
✅ CLOSED 2026-05-24.

**Effort**: 0.5 day.

---

## M8 — Bridge resilience under failing Discord logins (test-mode)

**Why**: discovered while debugging M7's CLI demo — when `index.ts`
runs `Promise.all([...adapters].map(a => a.start()))` and ALL discord
clients fail (e.g. fake tokens in dev), the rejection propagates
through `main()`, the process exits with rc=1, and the optional
`BRIDGE_E2E_TEST` HTTP server never gets a chance to bind. This breaks
the developer's "spin up the daemon with fake tokens just to talk to
the test endpoint" use case. In production we still want to fail-fast,
because a failed login there is a real config error.

**Scope**:
- Extract the wiring from `src/index.ts` into a new `src/bridge.ts`
  exporting `runBridge(opts) → Promise<{ shutdown }>`. `opts` accepts
  an `adapterFactory` for dependency injection so tests don't need to
  hit real Discord.
- In test mode (`opts.bridgeE2eTest === true`):
  - Start the test-injection HTTP server FIRST, before any adapter.
  - Wrap each adapter `start()` in its own try/catch — log failures,
    do not reject the outer promise.
- In production mode: keep current behaviour — `Promise.all` fans out
  the adapter starts; any rejection bubbles up.
- `src/index.ts` becomes the thin entry point: read env, call
  `runBridge`, wire SIGINT/SIGTERM to the returned shutdown handle.

**Tasks**:
- [x] `src/bridge.ts` — `runBridge(opts)` with `createAdapter`
      injection for tests. Test-mode and production-mode wiring split
      cleanly.
- [x] Secondary bug discovered + fixed via regression test: one shared
      dispatcher pointed its send-target at the Discord adapter,
      meaning `/_test/inject` replies tried to flow back through a
      not-logged-in Discord client (the send-queue's error handler
      swallowed the failure silently → GET `/_test/last-reply` returned
      404). Fix: two dispatchers sharing SessionManager +
      TurnMetaTracker, each with its own send-target. The discord
      dispatcher routes to DM channels; the test-injection dispatcher
      routes to `server.recordReply()`. Forward-reference for the test
      server.
- [x] `src/bridge.test.ts` — 6 cases:
      (a) BRIDGE_E2E_TEST=true + all adapters reject start() → resolves
          + test-injection server reachable;
      (b) BRIDGE_E2E_TEST=true + one adapter succeeds + one rejects →
          still resolves;
      (c) BRIDGE_E2E_TEST=false + an adapter rejects → rejects with
          best-effort teardown of partial state;
      (d) BRIDGE_E2E_TEST=false + all succeed → resolves, no test URL
          exposed;
      (e) shutdown() stops every adapter exactly once;
      (f) regression: end-to-end /_test/inject → /_test/last-reply
          round-trip records both authorised replies and unauthorised
          refusals (the secondary-bug guard).
- [x] Slim `src/index.ts` to ~30 LOC — just read env, call
      `runBridge`, wire SIGINT/SIGTERM to the shutdown handle.
- [x] Manual smoke: BRIDGE_E2E_TEST=1 daemon with two fake-token
      agents — bridge survives both TokenInvalid errors, CLI inject
      via `scripts/cerase-acp-cli inject --remote …` returns the
      fake-child reply for authorised users, the Italian refusal for
      unauthorised, and HTTP 500 for unknown agents (correct: an
      unknown agent is a programmer error from the ingress, not a
      user-facing condition).

**Exit gate**: `npm test` green (93/93); in test mode with all-fake
tokens the test server is reachable on :7474, inject round-trips
work, and shutdown is clean. In production mode the existing
fail-fast on token error is preserved with a best-effort teardown.
✅ CLOSED 2026-05-24.

**Effort**: 0.25 day → bumped to ~0.4 day because a secondary bug
surfaced during the manual smoke. Net: still cheaper than discovering
either bug in the cerase repo's e2e-discord tier.

---

## M9 — Agent cwd from agents.yaml (don't leak host / bridge cwd)

**Why**: surfaced during the M8 close manual smoke. `session-manager.ts`
was calling `connection.newSession({ cwd: process.cwd() })`, which
means the working directory advertised to the agent was always the
launching process's cwd — `/home/<user>/Documents/software/cerase`
when run via the host-side `./cli.sh`, `/app` when run as the bridge
daemon inside cerase-bridge. Neither path exists inside the agent
container (`cerase-agent-doc-qa`), where the agent's persistent
"shared desk" is mounted as a named volume. OpenCode silently
absorbs the bogus cwd into its session state, and when the user asks
"what folder are you working in?" the LLM happily echoes the host
path — surprising, confusing, and clearly wrong.

**Scope**:
- agents.yaml schema gains an optional `cwd: string` field per agent.
  Default `/root/cerase/workspace` — the canonical Cerase agent
  workspace path under the container's HOME (consistent with how
  kiso-run namespaces everything under `~/.kiso/`; gives us
  `~/cerase/data`, `~/cerase/cache`, etc. for free as we need them
  in later milestones).
- `session-manager.ts.spawnAndInit` reads `agent.cwd` and passes it
  to `newSession({ cwd })` instead of `process.cwd()`.
- `agents.yaml.example` documents the field + the default + the
  rationale.

This milestone is paired with a cerase-repo change (B0-followup) that
renames the agent container's volume mount targets from
`/var/lib/cerase-agent/{work,data}` to `/root/cerase/{workspace,data}`
so the default lines up with what the agent container actually
mounts. The two changes ship together because shipping one without
the other leaves a mismatch.

**Tasks**:
- [x] cerase-acp/src/config.ts — `AgentSchema` gains
      `cwd: z.string().min(1).default("/root/cerase/workspace")`.
- [x] cerase-acp/src/session-manager.ts — `spawnAndInit` passes
      `agent.cwd` to `newSession({ cwd })` instead of `process.cwd()`.
- [x] cerase-acp/src/config.test.ts — 2 new cases: default applied
      when `cwd` absent (×2 agents); explicit override round-trips.
- [x] cerase-acp/src/session-manager.test.ts — extended fake-acp-child
      to echo the received cwd back inside the sessionId string
      (`fake-session-cwd=<cwd>`), new case asserts the bridge passes
      `agent.cwd` (not `process.cwd()`) all the way to ACP `session/new`.
- [x] cerase-acp/agents.yaml.example — documents the new field, its
      default, and why passing the host cwd is wrong.

**Exit gate**: `npm test` green (96/96); pending the paired cerase
B0-followup that renames the agent container mount targets so the
default `cwd` lands on a path the agent container actually mounts.
✅ CLOSED 2026-05-24.

**Effort**: 0.25 day.

---

## Total effort (v0.1)

~5.5 days of focused work. Aligns with the cerase devplan §M4 estimate of
5-7 days (the extra 0-1.5 days live on the cerase side: B1 compose
wiring, B5 e2e-discord tier + automated tests, B6 manual validation +
devplan close).

---

## Out of scope for v0.1 (deferred)

- **Slack / Telegram adapters** — Tier-0 only, by customer request.
- **Multi-bot per agent** — one bot per template is the PoC contract.
- **Persistent session-store across container redeploys** — v0.1
  relies on mem0 + the persisted workspace inside the agent container
  to recover continuity on the next user turn. A dedicated
  session-store (à la OpenACP) is a v0.2 concern.
- **Skill command pinning, slash commands, button-based permission
  approval** — explicit UX-incompatibility per cerase M2 rules.
- **Doctor checks** — deferred; structured pino logs cover the PoC
  observability need.

---

## M10 — Loggers write to stderr, not stdout (`./cli.sh prompt | jq` works)

**Why**: discovered during the M9 manual smoke. The CLI was streaming
the LLM reply to stdout AND pino was writing structured logs to stdout
by default. Under normal usage the user just saw both intermixed
("agent requested permission in-DM — auto-cancelled" landed between
reply chunks); the worse scenario is any pipeline (`./cli.sh prompt
"…" | jq`, copy to clipboard, etc.) ingests the log lines.

**Scope**:
- Extract a tiny `src/logger.ts` exporting `makeLogger(name)` that
  passes `pino.destination(2)` so logs always land on stderr.
- Migrate every existing `const logger = pino({ name, level })` block
  (7 files: bridge, send-queue, discord-adapter, test-injection,
  dispatcher, session-manager, index) to `makeLogger("<same-name>")`.
  Single source of truth for the level + destination contract.

**Tasks**:
- [x] `src/logger.ts` — `makeLogger(name) → pino.Logger` with
      `pino.destination(2)`.
- [x] 7 call sites migrated. Stale per-file "level: silent honoured…"
      comment in session-manager removed (the contract now lives in
      logger.ts).

**Exit gate**: `npm test` green (96/96, unchanged); verified
end-to-end:
  $ ./cli.sh prompt "test" 2>/dev/null   # stdout only
  → reply only, no JSON log
  $ ./cli.sh prompt "test" 1>/dev/null   # stderr only
  → pino logs only, no reply text
✅ CLOSED 2026-05-24.

**Effort**: 0.1 day.

---

## M11 — CLI fallback: stream agent_thought_chunk when no agent_message_chunk arrives

**Why**: surfaced during the M10 manual smoke. Asked the agent
"ciao amico, come stai?" via `./cli.sh repl` — the REPL printed
nothing. Debug-log of every ACP `sessionUpdate` revealed the agent
emitted 4× `agent_thought_chunk` + 1× `available_commands_update`,
ZERO `agent_message_chunk`, `stopReason=end_turn`. The model
finished the turn but produced no user-visible message: everything
went into the thought channel.

Semantically, ACP separates "thought" (chain-of-thought, optionally
visible to the operator) from "message" (the canonical user-facing
reply). Our CLI filtered on `agent_message_chunk` only, which is
correct for the happy path — but when the agent emits zero message
chunks, falling back to displaying the accumulated thought is
better than showing the user nothing.

This is a model-quality workaround at the PoC scale (deepseek-v4-
flash via OpenRouter; stronger Tier-0 models are expected to use
the message channel reliably). Worth doing because zero output ⇒
the user retries the same prompt and gets the same nothing.

**Scope**:
- `src/cli.ts`: track separately the number of message-chunk text
  bytes emitted and the accumulated thought-chunk text. After
  `mgr.prompt()` resolves: if message-chunk bytes == 0 AND
  thought-chunk text is non-empty, stream the thoughts with a
  one-line preamble (`(no direct reply; here's the agent's thinking
  instead)`) on stderr so a `./cli.sh prompt … | jq` pipeline still
  only ingests the model's intended text, not the preamble.
- `src/__tests__/fake-acp-child.mjs`: gain a `FAKE_KIND=thought`
  knob that flips emission from `agent_message_chunk` to
  `agent_thought_chunk` for the same FAKE_REPLY text. Default
  remains `message` so the existing tests keep their semantics.
- `src/cli.test.ts`: 2 new cases — happy-path thoughts are NOT
  surfaced when a message exists; fallback path streams the
  thoughts when zero message chunks arrived.

**Tasks**:
- [x] `src/__tests__/fake-acp-child.mjs` gains `FAKE_KIND` env knob
      (`message` default | `thought`) → flips emission between
      `agent_message_chunk` and `agent_thought_chunk` over the same
      text payload.
- [x] `src/cli.ts` tracks messageBytes and thoughtFallback. After
      `mgr.prompt()` resolves, falls back to streaming thoughts
      when no message chunks were emitted. Preamble line on stderr
      so pipelines ingest only model text.
- [x] `src/cli.test.ts` — 2 new cases:
        - happy path (message chunks only) → stderr has no preamble
        - fallback path (thought chunks only) → stdout has the
          thought text + stderr has the preamble line
- [x] Investigation note: same model + prompt in `opencode` interactive
      shell DOES produce a message chunk; only in `opencode acp` mode
      does the model emit thought-only on short conversational
      prompts. Workaround at the CLI layer is the right place — the
      bug, if it is one, is opencode-acp-mode side and out of scope
      for v0.1.

**Exit gate**: `npm test` green (98/98); `./cli.sh prompt "ciao
amico, come stai?"` against the real cerase-agent-doc-qa now prints
the model's chain-of-thought on stdout + the preamble on stderr,
instead of a blank line. ✅ CLOSED 2026-05-24.

**Effort**: 0.2 day.

---

## M12 — Drain post-prompt stream (mitigate opencode upstream #17505 / #25421)

**Why**: M11 surfaced "empty reply" / "truncated reply" symptoms. The
M11 thought-fallback patched the user-visible result but missed the
root cause. M12 investigation via persisted LiteLLM payloads
(`LiteLLM_SpendLogs.response`, gated by `store_prompts_in_spend_logs:
true` in cerase's litellm/config.yaml) showed the model produces the
full `content` correctly — opencode-acp's RPC reply just races ahead
of the final `agent_message_chunk` notifications:

- Upstream: `anomalyco/opencode#17505` (root) — "session/update
  notifications sent after session/prompt response (end_turn)" —
  open, multiple downstream clients impacted (Fabriqa, agent-shell,
  JetBrains Rider).
- Upstream `#25421` (duplicate, more recent): "ACP
  agent_message_chunk frames land after end_turn RPC reply due to
  event-subscription / prompt-RPC race".

The bug violates the ACP spec ("Agent MUST send all session/update
notifications before responding to session/prompt"). Filed upstream,
no fix landed in opencode 1.15.10.

**Scope**: client-side drain in cerase-acp's SessionManager. After
`await connection.prompt(...)` resolves, the prompt() method holds
the active onUpdate hook open and idle-polls until either:
  (a) ≥`POST_PROMPT_IDLE_MS` (300ms) of no new updates has elapsed
      since the last received chunk, OR
  (b) `POST_PROMPT_MAX_DRAIN_MS` (2000ms) safety ceiling reached, OR
  (c) the ACP child exited (entry.closed → no more chunks possible)
Only then prompt() returns to the caller. Costs ~300ms of latency
per turn (one-time, after the model finishes generating) but
recovers the full reply.

**Tasks**:
- [x] `src/session-manager.ts.prompt()`: wrap `entry.onUpdate` to
      stamp `lastUpdateAt`. After `connection.prompt()` resolves,
      poll-loop with the three exit conditions above.
- [x] `src/session-manager.test.ts`: existing "respawns transparently
      after the child crashes" test updated — previously asserted
      `activeSessionCount() == 1` immediately after prompt() resolved.
      With the drain that's no longer true (child has exited before
      drain finishes when FAKE_CRASH_AFTER_PROMPT=1). New assertion:
      r2 returns end_turn and the manager handles the respawn-then-
      die loop without throwing.
- [x] Cross-confirmed via cerase/litellm.config.yaml — the
      `store_prompts_in_spend_logs: true` flag that made this
      investigation possible is now committed permanently (it's also
      the audit-log the customer is promised in cerase.ai positioning).

**Exit gate**: `npm test` green (98/98); end-to-end against the
running stack reproduces ALL three turns correctly:
  - "ciao amico, come stai?" → "Ciao! Sto benissimo, grazie..."
  - "che skill hai?" → full skill description
  - "spiegami in due frasi cos'è git rebase" → 2 articulate sentences

✅ CLOSED 2026-05-24.

**Effort**: 0.2 day (most of it was the investigation — fix itself
is ~15 LOC).

---

## M14 — Quiet the permission-denied log at default level

**Why**: spotted while testing the REPL. When the agent triggers a
`requestPermission` ACP call and the bridge auto-cancels (M2 UX
rule), session-manager.ts emitted a pino `logger.warn`. Default
CLI `--log warn` lets warn-level through; result was the JSON log
mixed into the streamed `thinking:` block on stderr:

  thinking: The user wants me to list the root …{"level":40,…,"msg":"agent requested permission in-DM — auto-cancelled (PoC policy)"}

Lowered to `logger.info`. At default warn-level the log is hidden;
operators investigating retry-loops `--log info` and see them.

**Scope**: single-line edit in `src/session-manager.ts`. Behaviour
unchanged (still auto-cancels), only severity shifted.

**Exit gate**: ./cli.sh repl asks "ls /" → no JSON log leaks into
the thinking block. `--log info` brings it back for debugging.
✅ CLOSED 2026-05-24.

**Effort**: 5 minutes.

---

## M13 — In-process TS REPL (mirror Discord daemon lifecycle)

**Why**: the user spotted the bug while testing — REPL multi-turn
made the agent answer as if every turn were the first
("non ho detto di parlare in un'altra lingua. Questa è la prima
interazione"), with the model's own reasoning correctly noting
`gap=first` on each turn. Root cause: the bash REPL we shipped in
M7 spawned a fresh `node dist/cli.js prompt` per stdin line, which
created a new SessionManager → new opencode-acp child →
`session/new` → fresh sessionId every turn. Each turn was
genuinely a new conversation from ACP's perspective.

The architectural insight (user's): the value of `./cli.sh repl`
is to mirror what a Discord user experiences with the production
daemon — same logic, same problems, same debug surface. The
production daemon keeps ONE SessionManager + ONE child per
`(agent, user)` alive across N DMs; the CLI repl must do the
same to be a faithful proxy. Anything else (state-on-disk
session resume, etc.) tests a fictional pipeline.

Reverses the M7 decision ("complessità fuori, bash gestisce il
REPL"). Worth the revert because the previous design produced
a debugging tool that gave wrong answers.

**Scope**:
- New TS subcommand `node dist/cli.js repl --config X --agent Y --user Z`:
  - Setup once: load config, validate agent, allowlist check (refuse
    once → exit 0 with the polite refusal, like `prompt`),
    instantiate ONE SessionManager + ONE TurnMetaTracker
  - Loop with `readline`: each stdin line fires
    `mgr.prompt(agentId, userId, prefix+line, onUpdate)` against the
    persistent SessionManager. Lazy spawn on first turn; child
    stays alive through subsequent turns; TurnMetaTracker remembers
    last-turn-at so gap goes from "first" → real durations on
    turn 2+.
  - Empty stdin line, EOF (Ctrl-D), or SIGINT → mgr.shutdown() →
    exit 0.
  - Same thought+message streaming as `prompt` subcommand
    (extracted into a shared helper).
- Refactor: extract `streamPromptResult(...)` and `loadAndValidate()`
  helpers so `prompt` and `repl` share the boilerplate (config
  load → agent validation → allowlist → tracker construction →
  per-turn streaming with messageBytes/thoughtBytes/marker logic).
- Bash wrapper `cmd_repl` collapses to `exec node "$_TS_CLI" repl "$@"`.
  All the loop logic moves into TS.
- Test: cli.test.ts gains a `repl` case that drives stdin through
  `runCli` with a multi-line buffer + asserts that turn 2's
  `[turn_meta]` prefix carries a non-`first` gap (proves the
  TurnMetaTracker persisted across turns). Plus an assertion that
  the SessionManager still has 1 active session after turn 2
  (proves the child was reused).
- Update cli-wrapper.test.ts REPL case if it relies on per-turn
  spawn behaviour.
- README "Debug CLI" section: remove the misleading "Each turn = a
  fresh ACP session" caveat. Now: "REPL keeps one ACP child alive,
  matching the production Discord daemon's lifecycle for the
  matching `(agent, user)`."

**Tasks**:
- [x] Refactor cli.ts: extracted `parseCommonArgs`, `loadAndValidate`,
      `runOneTurn` helpers. Both `runPrompt` and `runRepl` share them.
- [x] `prompt` subcommand: load+validate, one SessionManager+Tracker,
      one runOneTurn, shutdown.
- [x] `repl` subcommand: load+validate, persistent SessionManager+
      Tracker, readline loop (default uses node:readline on stdin;
      tests inject a custom `readLine` async function via `CliIO`),
      runOneTurn per line, shutdown on empty line / EOF.
- [x] cli.test.ts: 5 new repl cases — multi-turn continuity,
      tracker persistence, empty stdin, unauthorised refusal,
      unknown agent.
- [x] scripts/cerase-acp-cli: `cmd_repl()` collapsed to
      `exec node "$_TS_CLI" repl "$@"`.
- [x] cli-wrapper.test.ts: REPL case retargeted to the TS-in-process
      contract.
- [x] README "Debug CLI": REPL comment updated to "single ACP child
      kept alive across all turns (mirrors Discord daemon)".

**Exit gate**: `npm test` green (103/103); `./cli.sh repl` against the
real cerase-agent-doc-qa preserves conversation history across turns.
Verified end-to-end:
  > mi chiamo Paolo                 → "Ciao Paolo, come posso aiutarti?"
  > come mi chiamo?                 → "Paolo." (with thinking confirming
                                       "I know they told me their name
                                       is Paolo in the previous message")
  > che skill hai?                  → full skill description
✅ CLOSED 2026-05-24.

**Effort**: 0.4 day.

---

## M15 — Drain budget bump + per-turn timing telemetry (mitigation)

**Why**: M12 closed with `POST_PROMPT_IDLE_MS=300ms` /
`POST_PROMPT_MAX_DRAIN_MS=2000ms`. Subsequent end-to-end testing
(2026-05-24, "ls /home" turn with tool-call followed by text reply)
produced empty visible replies despite the model emitting a full
`content` field — confirmed by reading `LiteLLM_SpendLogs.response`
for the offending turn. Hypothesis: in turns with a tool-call
intermediate, the final `agent_message_chunk` lands beyond our 2s
ceiling because the SSE delta stream (subscribed via the opencode
SDK) is racing against the `session/prompt` RPC reply (upstream
race documented in `anomalyco/opencode#17505`; PR `#21316` by
LydiaCai1203 implements the canonical fix server-side but was
auto-closed by the github-actions compliance bot 2 hours after
filing, never resubmitted).

M15 is the **cheap mitigation tier** — pure config + telemetry, no
algorithmic change. Buys breathing room before the M16 serious fix
lands, and the telemetry survives M16 as regression-detection.

**Scope**:
- `src/session-manager.ts`: bump `POST_PROMPT_MAX_DRAIN_MS` from
  `2000` → `8000`. Keep `POST_PROMPT_IDLE_MS=300` (post-tool turns
  with text streaming typically have <100ms inter-chunk gaps once
  text starts flowing — the 300ms idle exit is fine).
- Per-turn instrumentation emitted at `logger.info` level via a
  new `[turn_telemetry]` line in `prompt()` after drain completes:
  ```
  [turn_telemetry] sessionId=… chunks_received=N text_chunks=M
    thought_chunks=K last_chunk_age_ms=X drain_exit=idle|ceiling|closed
    prompt_to_end_turn_ms=… end_turn_to_drain_done_ms=…
  ```
  `prompt_to_end_turn_ms` is the time `connection.prompt()` took
  to resolve. `end_turn_to_drain_done_ms` is the drain phase
  itself. Operators can grep `[turn_telemetry]` from pino stderr
  to dimension the bug ("are we hitting the 8s ceiling? was the
  last chunk emitted at 4s? 6s?").
- One CLI demo: `./cli.sh repl --log info` shows the
  `[turn_telemetry]` line after each turn (the demo path).

**Tasks**:
- [x] `src/session-manager.ts`: bump constant `POST_PROMPT_MAX_DRAIN_MS=8000`.
- [x] `src/session-manager.ts`: introduce `TurnTelemetry` interface +
      optional `SessionManagerOptions.onTelemetry` hook. Counters
      accumulated by the existing `entry.onUpdate` hook
      (`chunksReceived`, `textChunks`, `thoughtChunks`); timestamps
      `t0/t1/t2`; drainExit tag tracks which branch closed the
      loop. Emitted as one `[turn_telemetry]` pino info log per
      turn AND via the `onTelemetry` callback.
- [x] `src/__tests__/fake-acp-child.mjs`: replaced single-chunk
      late knob with `FAKE_LATE_BURST_TEXT` + interval, emitting
      one chunk per char post-end_turn. More faithful to the
      upstream race shape.
- [x] `src/session-manager.test.ts`: new test "captures a 3s burst
      of late chunks after end_turn" — 30-char burst at 100ms
      intervals = 3000ms post-end_turn total. Pre-M15 ceiling cut
      at 2000ms losing the tail; new ceiling captures all 30.
- [x] `src/session-manager.test.ts`: new test "emits per-turn
      telemetry via the onTelemetry hook" — asserts shape of
      captured `TurnTelemetry`.
- [x] No CLI surface change. No README change (telemetry is for
      operators, not the user-facing repl docs).

**Exit gate**:
- `npm test` green (105/105).
- `./cli.sh repl --log info` against running stack: end-to-end
  validation in M16 (where we'll have the reconciler to compare
  results against). M15 in isolation reliably handles the
  burst-after-end_turn case but does NOT fix the "single
  delayed chunk after quiet" case — that's M16 territory.

**Effort**: 0.2 day (actual: 0.15 day).

**Disposability**: when M16 lands or upstream merges a real fix,
delete the drain constants and the drain loop entirely; KEEP the
`[turn_telemetry]` log — it's free regression detection.

✅ CLOSED 2026-05-24.

---

## M16 — Shadow-channel REST reconciliation (serious client-side fix)

**Why**: M15 narrows the timing window but does NOT eliminate the
race — chunks emitted after the 8s ceiling, or duplicated, or
re-ordered relative to `tool_call_update` events still corrupt the
visible reply. The structurally clean fix is what upstream PR
`anomalyco/opencode#21316` proposed and never merged: after
`session/prompt` returns end_turn, fetch the canonical assistant
message via REST and reconcile any text/reasoning content the
delta stream missed.

We implement this at the **cerase-acp client layer** rather than
forking opencode because:

1. We avoid a binary fork burden (rebase per `OPENCODE_VERSION`
   bump in `cerase/.env`).
2. `opencode serve`'s REST endpoint is already running and
   healthchecked in our compose stack (`cerase-agent-{template}`
   container exposes `:3284` with HTTP basic auth). The REST DB
   (`opencode.db`, SQLite WAL) is the authoritative audit log
   we ship to customers (cerase.ai positioning promise). Reading
   from it for reconciliation is "free" — the infrastructure
   exists for an independent reason.
3. When upstream merges a real fix, ~50 LOC of reconciliation
   collapse cleanly; the REST polling/persistence half stays —
   we want it anyway for the audit channel.

**Scope**:
- New module `src/reconciler.ts`:
  - `fetchAssistantMessage(agent, sessionId, messageId): Promise<MessageInfo>`
    hits `GET http://cerase-agent-{template}:3284/session/{sessionId}/message/{messageId}`
    with the HTTP basic auth credentials (`OPENCODE_SERVER_PASSWORD`)
    already wired into the bridge container's env.
  - Endpoint host derived from `agents.yaml` entry: each agent gets
    a new optional field `restEndpoint: { baseURL, username, password }`
    (default `{ http://cerase-agent-{id}:3284, "opencode", env.OPENCODE_SERVER_PASSWORD }`)
    so the agent record stays self-describing.
  - `reconcile(seenChunks: SeenChunks, message: MessageInfo): ReconciledDelta[]`
    diffs what we received via ACP vs the final canonical content
    and produces synthetic deltas to emit, with a `reconciled: true`
    marker on each.
- `src/session-manager.ts.prompt()`:
  - During drain, accumulate per-part offset counters
    (`textCharsSeen[partId]`, `reasoningCharsSeen[partId]`).
  - After drain exits, if `prompt_response.data?.info?.id` is
    present (the assistant messageId), call `fetchAssistantMessage`
    + `reconcile`. For each synthetic delta, invoke
    `entry.onUpdate({ sessionUpdate: "agent_message_chunk" | "agent_thought_chunk",
       content: { type: "text", text: delta }, _reconciled: true })`.
  - Only ONLY emit if `delta.length > 0` (avoid noise when drain
    captured everything).
  - Add per-turn telemetry: `reconciled_text_bytes=N reconciled_thought_bytes=M`
    to the `[turn_telemetry]` line.
- `src/cli.ts` streaming helper:
  - When a `_reconciled: true` chunk surfaces, prefix the visible
    line with a faint `[recovered from audit log]` marker once per
    turn (not per chunk). Customer-trust signal: "we noticed
    transport dropped some, we recovered it from the persisted
    record."
- `agents.yaml` example + schema validation: `restEndpoint` field
  optional with defaults; existing entries without it work
  unchanged.

**Tasks**:
- [x] `src/reconciler.ts`: pure-function diff. Concatenates
      text and reasoning parts (skipping `ignored: true` text
      parts to match opencode's own delta emitter at
      agent.ts:466) and returns the suffix the ACP stream
      missed. Conservative on out-of-sync states: if seen >
      canonical, returns empty.
- [x] `src/opencode-rest.ts`: REST fetcher with HTTP basic auth
      (username "opencode" hardcoded by opencode serve;
      password from `OPENCODE_SERVER_PASSWORD` env). 2s
      timeout. Returns `null` on 404 / non-2xx / network
      error — never throws to caller, which means
      reconciliation degrades silently.
- [x] `src/opencode-rest.ts.defaultEndpointForAgent()`: builds
      `http://cerase-agent-{id}:3284` from the agent.id
      convention. Returns `null` if env var missing (test
      / host shell), in which case M16 is inert.
- [x] Vitest: `reconciler.test.ts` — 8 pure function tests
      covering equal/prefix-shorter/multi-part/ignored/longer-
      than-canonical/non-text-parts cases.
- [x] Vitest: `session-manager.test.ts` — 4 new M16 cases:
      reconciles tail when messageId present; degrades to
      no-op when child emits no messageId; skips when
      endpointResolver returns null; degrades gracefully
      when fetcher throws.
- [x] `src/__tests__/fake-acp-child.mjs`: optional
      `FAKE_MESSAGE_ID` env var so test fixtures can mimic
      production opencode-acp behaviour (always attaches
      messageId to chunks).
- [x] `src/session-manager.ts`: integrated reconciler into
      `prompt()` flow. Per-turn `seen: SeenState` accumulator
      + `assistantMessageId` latch from the first chunk that
      carries it. After drain, fetch + reconcile + replay
      synthetic chunks via `onUpdate`. Telemetry now carries
      `reconciledTextBytes` and `reconciledReasoningBytes`.
- [x] `src/cli.ts`: once-per-turn recovery marker printed to
      stderr (dim) when `last.reconciledTextBytes > 0 ||
      reconciledReasoningBytes > 0`. Tracks last telemetry via
      a small `telemetrySink` object passed through to
      `runOneTurn`.
- [-] `agents.yaml` schema: deferred. The current
      `defaultEndpointForAgent(id)` convention + env-based
      auth covers our docker compose layout cleanly. Adding
      a yaml field complicates the schema without buying us
      anything until we have an agent NOT following the
      `cerase-agent-{id}` naming. Revisit if needed.
- [-] CLI integration test for the recovery marker: deferred.
      Requires plumbing a fake fetcher through the CLI
      surface, which would balloon API. The path is exercised
      end-to-end in `session-manager.test.ts`; the CLI's
      marker code is straight-line `if reconciledBytes > 0
      → print`. Acceptable test gap.
- [x] README touch deferred to M17 once we have the upstream
      story to point at.

**Exit gate**:
- `npm test` green (117/117).
- End-to-end against running stack:
  - `./cli.sh repl` over a multi-turn conversation including the
    "ls /home" repro that exposed M15's residual gap — visible
    reply matches the canonical LiteLLM payload character-for-
    character.
  - When ACP delivers everything correctly: no marker, telemetry
    shows `reconciledTextBytes=0`.

**Effort**: 1.0 day (actual: ~0.6 day).

**Disposability stance**: when upstream merges a real fix,
delete the reconciliation diff/emit logic (~50 LOC in
session-manager prompt() + reconciler.ts entirely); KEEP
`opencode-rest.ts` — it's the audit channel we promise
customers anyway, and other features (session export, message
inspection) will lean on it.

✅ CLOSED 2026-05-24.

---

## M17 — Upstream PR to sst/opencode (bonus, post-M16)

**Why**: PR `anomalyco/opencode#21316` was auto-closed by the
github-actions compliance bot 2h after filing because the PR
description didn't match the template. Author (LydiaCai1203)
never resubmitted. The fix logic was correct (serialized
sessionUpdate chain + post-prompt reconciliation), it just
died on procedural friction. Reopening with a properly
formatted PR is ~2h of work and unblocks every other ACP
client.

Done AFTER M16 because (a) our reconciliation experience
informs the PR, (b) M16 derisks us so the PR isn't on our
critical path.

**Scope**:
- Fork `sst/opencode` at the latest released tag (current pin:
  `OPENCODE_VERSION=1.15.10` in `cerase/.env`).
- Port the algorithm from PR `#21316` to current `agent.ts`
  (significant refactor since v1.2.21: see commits `2fdee50
  refactor(acp): extract runtime reentry` and `0956b15
  refactor(acp): drop async from synchronous ACP.init`).
  Two parts of the fix:
  1. Serialized `sendChain` for all `sessionUpdate` calls
     (the out-of-order emission half).
  2. `reconcileAssistantMessage()` call before `return end_turn`
     in `agent.ts:prompt()` (the late-chunk half).
- PR description carefully formatted per `CONTRIBUTING.md` +
  PR template — checklist items, type of change, repro steps,
  verification.
- Reference `#17505` (the root issue) AND `#21316` (the
  previous attempt, credit LydiaCai1203 in the description).
- Include vitest coverage demonstrating the fix.

**Scope reduction (decided 2026-05-24)**: instead of opening a
new PR upstream we filed a substantive comment on the canonical
open issue `anomalyco/opencode#17505` with (a) the 2026 v1.15.10
repro evidence including the LiteLLM canonical payload diff,
(b) a pointer to the prematurely auto-closed PR #21316, and
(c) a ready-to-apply ~30 LOC patch sketch for current
`agent.ts:prompt` that closes the issue. Comment URL:
<https://github.com/anomalyco/opencode/issues/17505#issuecomment-4529059211>.

The full PR was deferred because:
- The fix code from #21316 is already public; reposting it as a
  fresh PR adds little above asking maintainers to revive the
  existing closed PR (less procedural friction for everyone).
- M16 already covers our production needs client-side. Opening
  a PR would have meant ~6h of Bun toolchain setup + porting +
  upstream review cycle, without commensurate value to us.
- A high-signal issue comment with concrete fix algorithm gets
  the same community impact as a PR (any contributor can lift
  the sketch into a PR if they have the upstream-build setup
  ready), at a tenth of the cost.

**Tasks**:
- [x] Investigated upstream issue/PR catalog. Found #17505
      (the canonical race) still OPEN, and #21316 (the fix)
      auto-closed by github-actions for template format.
- [x] Drafted comment combining: confirmation on v1.15.10,
      LiteLLM canonical payload diff vs ACP-visible reply,
      pointer to #21316, 30-LOC patch sketch for `agent.ts`,
      and a client-side mitigation algorithm for downstream
      consumers.
- [x] Posted comment as Ymx1ZQ on
      `anomalyco/opencode#17505` (irreversible / public —
      reviewed-and-approved before submission).
- [-] Open full upstream PR — DEFERRED. Will be reconsidered if
      maintainers respond asking for one, or after several
      weeks of production telemetry confirms M16
      reconciliation actually fires (i.e. the race still hits
      real traffic, not just synthetic test fixtures).

**Exit gate**:
- Comment is live and authored by Ymx1ZQ on the canonical
  open issue. ✓ verified via `gh api
  /repos/anomalyco/opencode/issues/17505/comments`.

**Effort**: 0.5 day → actual 0.1 day. Most of the time went
into picking the right venue (new issue vs comment) and
preparing the patch sketch with accurate v1.15.10 line
references.

✅ CLOSED 2026-05-24.

---

## M18 — Discord "is typing…" indicator during prompt processing

**Why**: User-facing latency on a typical Spark-tier turn is 5–15s
(LLM round-trip with possible tool-call intermediates). During
that window Discord shows nothing — the user can't tell if the
bot received the message or if something's stuck. Native typing
indicators ("Claudia is typing…") are the canonical Discord UX
pattern for this exact situation; cheaper and less cluttering
than emoji reactions (👀 / ✅ / ❌), and self-clearing when the
real message lands.

**Scope**:
- New helper `src/typing-keepalive.ts`. Pure-logic module so it
  unit-tests cleanly without mocking discord.js:
  - `startTypingKeepalive(target, options?) → stopFn`
  - `target` is a minimal interface `{ sendTyping(): Promise<unknown> }`.
    Discord.js's `DMChannel` / `TextChannel` already match.
  - Calls `target.sendTyping()` immediately + every
    `intervalMs` (default 7000 — Discord auto-stops the
    indicator after 10s).
  - Defensive ceiling `maxTicks` (default 42 → ~5 minutes).
    Hung turns shouldn't leave the indicator running forever.
  - Returns a `stopFn` that clears the interval.
- `src/discord-adapter.ts`:
  - On `MessageCreate` (after the early "is bot / is DM" guards
    pass but BEFORE the dispatcher call), invoke
    `startTypingKeepalive(msg.channel)`.
  - Wrap `dispatcher.handleMessage` in try/finally so the
    `stopFn` always fires (success path, dispatch throw,
    allowlist refusal — all paths).
  - In `makeSendTarget`, after each `channel.send(chunk)`,
    also call `channel.sendTyping()` to eliminate the
    visible gap between an intermediate Discord message
    (which auto-clears the indicator) and the next keepalive
    tick. Documented tradeoff: this triggers one last
    "is typing…" after the FINAL send too, visible for up
    to ~10s before Discord's own auto-stop. Acceptable —
    Discord users routinely see this pattern when humans
    start typing then change their mind. Cheap UX cost vs
    the alternative gap of up to 7s mid-turn.

**Tasks**:
- [x] `src/typing-keepalive.ts`: helper module per scope above.
      Pure-logic, Discord-agnostic at the type level — accepts
      a `{ sendTyping(): Promise<unknown> }` shape so vitest
      drives it without mocking discord.js.
- [x] `src/typing-keepalive.test.ts` (vitest): 5 cases —
      immediate call, periodic call, stopFn halts, maxTicks
      ceiling, swallows rejections without unhandled-promise.
- [x] `src/discord-adapter.ts`: `startTypingKeepalive(msg.channel)`
      in `MessageCreate` with try/finally guarding the
      dispatcher call. After each `channel.send(chunk)` in
      `makeSendTarget`, also `channel.sendTyping().catch(()=>{})`
      to close the gap between intermediate Discord messages
      and the next keepalive tick. Narrowed channel type via
      `"sendTyping" in msg.channel` check (the discord.js
      union includes `PartialGroupDMChannel` which lacks
      `sendTyping`).
- [x] `src/discord-adapter.ts`: also `msg.react("👀")` immediately
      after the early guards pass and before the typing keepalive
      starts — a persistent "I saw your message" trace that
      complements the transient typing indicator. Survives in
      the channel history after the turn completes. `.catch` on
      the react promise so a blocked-bot / deleted-channel
      transient never crashes the handler.
- [x] No new dependencies. No new Discord permissions
      (`sendTyping` is part of `Send Messages` already granted).

**Exit gate**:
- `npm test` green (vitest, all suites).
- Manual: DM Claudia in Discord with a question that takes
  >5s to answer. Discord shows "Claudia is typing…" within
  ~1s of sending. Indicator stays on through the whole turn
  (refreshed every 7s). When the reply lands, Discord clears
  the indicator. If the turn produces multi-message output
  (>2000 char total → SendQueue chunks across multiple
  Discord messages), the indicator reappears between
  intermediate messages within ~1s.

**Effort**: ~0.25 day (small helper + 1 wiring point + 5
vitest cases). Actual: ~0.2 day.

**Disposability stance**: helper is small + self-contained
and we'd keep it regardless of architecture pivots — DMs
need typing indicators in every reasonable UX direction.

✅ CLOSED 2026-05-24. Manual visual verification owed (Paolo
DMs Claudia/Luigi after the bridge image rebuild; sees
"is typing…" within 1s of sending, sustained through the
turn, cleared on reply landing).

---

## M19 — Auto-approve ACP permission requests (DM-only agents trust the container)

**Why**: M14 set the policy "auto-cancel every permission request
the agent makes via ACP, because we have no in-DM UI to ask the
user". That was correct for the M14 reasoning context (no
permission UI ergonomics in DMs). But empirical observation on
2026-05-24 showed that the LLM does NOT gracefully re-route when
a tool errors with `"The user rejected permission to use this
specific tool call."` — instead it interprets that exactly as
written and emits no follow-up text. From Discord's perspective
the turn goes silent.

Concrete repro (Luigi/policy-qa, 18:34): user asked
"haisalvato qualcosa in home?". OpenCode decided to call `read`
on `/home`. Permission request → cerase-acp auto-cancelled →
tool errored with "user rejected" → model gave up silently
(0 text chunks, 15 thought chunks, no further LLM call —
verified via LiteLLM_SpendLogs).

Root cause is upstream behavioural: opencode's session loop does
continue after a tool error in principle (we read
`packages/opencode/src/session/prompt.ts` lines 1267–1277 — the
exit check requires `!hasToolCalls`, which is false when a tool
was attempted), but the model decides to stop on its own when
told the user rejected its request. That's a sane model
behaviour — humans rejecting a tool means "stop". The fix
belongs at OUR layer: don't reject in the first place.

DM-only agents already have a strong security boundary: the
container sandbox + non-root `agent` uid (M16/B3) + read-only
config mount. The per-tool permission UI is for IDE clients with
interactive humans who want fine-grained control. In a Discord
DM context the user has already trusted the agent at the
"this agent gets to use its tools" level by opening a DM with
it — repeating that approval per-tool is friction without
upside.

**Scope**:
- New helper `src/permission-policy.ts`:
  - `decidePermissionOutcome(request: acp.RequestPermissionRequest):
    acp.RequestPermissionOutcome`
  - Prefer `allow_always` over `allow_once` (so subsequent
    identical tool calls don't re-prompt within the same
    session — saves round-trips).
  - Fall back to `allow_once` if `allow_always` not offered.
  - If neither is offered (defensive — would mean a future
    opencode version only proposes reject options): return
    `cancelled` and log a warning so we notice the regression
    in production telemetry.
- `src/session-manager.ts:requestPermission`: replace the M14
  auto-cancel body with `return { outcome: decidePermissionOutcome(params) }`.
  Keep the existing pino log line so operators can grep
  permission activity in production logs.

**Tasks**:
- [x] `src/permission-policy.ts`: pure helper per scope above.
- [x] `src/permission-policy.test.ts`: 5 vitest cases (prefer
      always, fall back to once, defensive cancel, empty list,
      ignore unknown kinds).
- [x] `src/session-manager.ts`: wire the helper into the
      `requestPermission` handler. Log message changed to
      "auto-decided via permission-policy" so log-grepping
      distinguishes the M19 regime from the M14 one. Log
      payload now includes the chosen outcome
      (`selected:<optionId>` or `cancelled`) for forensic
      visibility.
- [x] No existing test asserted on the auto-cancel outcome
      (M14 test only verified the log severity dropped from
      warn → info, not the response body), so no test
      update needed.

**Exit gate**:
- `npm test` green.
- Tsc clean.
- Deployed bridge image. Repro DM "haisalvato qualcosa in
  home?" to Luigi (workspace empty) → bot replies with a real
  message (e.g. "non c'è nulla nella directory") instead of
  going silent.
- `LiteLLM_SpendLogs` for that turn now shows ≥3 LLM calls
  (title + initial tool_calls + post-tool follow-up with text)
  vs the pre-M19 pattern of 2 calls.

**Effort**: ~0.2 day. ~30 LOC across two files + 5 vitest cases.

**Disposability stance**: permanent. The "container is the
boundary" stance is the long-term Tier-0 architecture for
Discord delivery (and the existing M16 reconciliation already
assumes per-agent REST/audit at the container level). Even
when an interactive UI surface lands (M5 Filament), DM-only
agents stay auto-approve — Filament adds approval flows for
the audit/override channel, not for the chat path.

