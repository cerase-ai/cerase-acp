# Devplan — cerase-acp v0.1 (PoC milestone M4)

**Status**: code-complete — live verification only
**Started**: 2026-05-24
**Umbrella milestone**: `cerase/devplan/poc.md` §M4 — `cerase-acp` (in-house DM connector) + OpenCode agent + DM round-trip

The PoC slice of `cerase-acp` is **code-complete**. Every milestone (M1–M19,
M21–M24 — there is no M20) is closed with green suites (vitest, tsc, biome).
The shipped record — one row per milestone — lives in
**[`poc-completed.md`](poc-completed.md)**; full prose detail is retained in
git history. The only work remaining in this file is operator-gated **LIVE
verification** of the most recent milestones (no open code).

---

## Operator-gated LIVE verification remainder

- [ ] **M18 — Discord "is typing…" indicator.** After a bridge rebuild, DM an
  agent (Claudia/Luigi) and confirm the indicator appears within ~1s, persists
  through the turn (refreshed every 7s), and clears when the reply lands.

- [ ] **M21 — README onboarding & Discord setup guide.** A fresh dev follows
  the README from zero to a Discord bot replying to DMs, unaided (docs
  walkthrough — every command, flag, env var, and config key checks out).

- [ ] **M22 — Production bridge resilient to a single adapter start failure.**
  With agent-1's Discord token still invalid, `/internal/status` stays up
  continuously (no crash-loop; `restarts` stops climbing) and a real inject to
  the web Manutentore (agent-2) returns 2xx and the assistant replies. Needs
  `./cli.sh build cerase-acp` + `./cli.sh restart cerase-acp`.

- [ ] **M23 — Auto-heal a failed adapter.** Kill the network to Discord, then
  restore it — the Discord adapter reconnects on its own within one backoff
  window (no container restart, no operator action) and `/internal/status`
  flips that agent back to `ready:true`. Needs a deployed bridge image.

- [ ] **M24 — Truthful container healthcheck (`/healthz`).** `docker inspect
  cerase-acp` health tracks the bridge actually being up (goes unhealthy if the
  internal server is down). Partially depends on the cerase-core half
  (`M-ACP-HEALTHCHECK-1` compose `test:` rewire) being deployed.

---

## Out of scope for v0.1 (deferred)

- **Slack / Telegram adapters** — Tier-0 only, by customer request.
- **Multi-bot per agent** — one bot per template is the PoC contract.
- **Persistent session-store across container redeploys** — v0.1
  relies on mem0 + the persisted workspace inside the agent container
  to recover continuity on the next user turn. A dedicated
  session-store (à la OpenACP) is a v0.2 concern.
- **Skill command pinning, slash commands, button-based permission
  approval** — explicit UX-incompatibility per cerase M2 rules.
- **Doctor checks** — deferred; structured pino logs cover the PoC
  observability need.
